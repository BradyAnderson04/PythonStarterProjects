# 2 sets of rules

def outloud(num):
    # check how many digits
    length = len(str(num))
    # generate rules for hundreds and ones
    print(str(num)[0])
    # generate rules for ten place

    #concatenate and return

print(outloud(900))